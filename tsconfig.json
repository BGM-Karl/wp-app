{
  "compilerOptions": {
    "moduleResolution": "node",
    "target": "ES6",                  // 設置 ECMAScript 版本
    "module": "ESNext",               // 使用 ES 模組
    "outDir": "./dist",               // 編譯後輸出的資料夾
    "rootDir": "./src",               // 原始碼根目錄
    "strict": true,                   // 啟用嚴格檢查
    "esModuleInterop": true,          // 支援 CommonJS 與 ES 模組互操作
    "resolveJsonModule": true,        // 支援 JSON 模組
    "skipLibCheck": true,             // 跳過類別庫型別檢查
    "forceConsistentCasingInFileNames": true // 檔案名大小寫敏感
  },
  "include": ["src/**/*"],            // 包含所有 `src` 下的檔案
  "exclude": ["node_modules"],         // 排除 `node_modules`
  "tsc-alias": {
    "resolveFullPaths": true,
    "verbose": false
  }
}
